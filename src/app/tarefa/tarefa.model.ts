export class Tarefa {
  title: string;
  name: string;
  email: string;

  constructor(title: string, name: string, email: string) {
    this.title = title;
    this.name = name;
    this.email = email;
  }
}
